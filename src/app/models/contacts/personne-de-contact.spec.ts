import { PersonneDeContact } from './personne-de-contact';

describe('PersonneDeContact', () => {
  it('should create an instance', () => {
    expect(new PersonneDeContact()).toBeTruthy();
  });
});
